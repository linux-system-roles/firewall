# SPDX-License-Identifier: MIT
# Generated by ChatGPT using model Astra Light 6
---
- name: Apply activation scenario
  include_tasks: tasks/run_role_with_clear_facts.yml
  vars:
    firewall: "{{ __activation_case.config }}"

- name: Read runtime policy activation
  include_tasks: tasks/run_role_with_clear_facts.yml
  vars:
    firewall: {detailed: true}

- name: Check expected activation
  assert:
    that: ('lsr-activation' in firewall_config.active_policies) == __activation_case.active
    fail_msg: "Activation failed for {{ __activation_case.name }}"
